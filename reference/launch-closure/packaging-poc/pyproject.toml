[build-system]
requires = ["maturin==1.15.0"]
build-backend = "maturin"

[project]
name = "pvfactors-packaging-probe"
version = "0.0.0"
requires-python = ">=3.11,<3.15"
dependencies = ["numpy>=2.3.4,<3"]

[tool.maturin]
bindings = "pyo3"
