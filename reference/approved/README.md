# Approved Geometry Golden

Status: **APPROVED**.

`geometry-golden-v0.1/` is the immutable G2 Geometry baseline approved by the
Repository Owner on 2026-09-22. Its `manifest.json`, `validation-summary.json`,
and 165 payload artifacts are byte-identical to the approved source candidate.
`APPROVAL.json` records the governance decision separately so promotion does
not rewrite the approved payload or its source manifest.

The approved source manifest SHA-256 is
`efd2adf3037740b9788792c9f5be6122fb2e842d72f2ddbe2bb5552abc27141b`.

Candidate generation and test failures must never update this directory.
Changing any approved file or manifest hash creates a new approval object and
requires a new explicit Repository Owner approval.
